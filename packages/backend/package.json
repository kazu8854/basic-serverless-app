{
  "name": "@basic-serverless-app/backend",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev:mock": "MOCK_AWS=true node --require ts-node/register src/dev.ts",
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "hono": "^4.2.4",
    "@basic-serverless-app/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "ts-node": "^10.9.2",
    "@types/node": "^20.0.0",
    "esbuild": "^0.20.0",
    "@types/aws-lambda": "^8.10.137"
  }
}
